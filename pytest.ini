[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = .
# test_integration.py 需要活服务器 (localhost:8000) — 常规跑测排除, 部署后手动:
#   python -m pytest tests/test_integration.py -q
addopts = --ignore=tests/test_integration.py
